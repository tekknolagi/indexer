========
Indexer
========

It's pretty simple.

Requirements:

* rubygems
  * sinatra
  * sqlite3
  * erb

Usage:

* To create the db and file folder, run `ruby indexer.rb create`
* To start the server, run `ruby indexer.rb`